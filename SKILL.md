# SKILL.md — נהלי בנייה לאתר Smart Scale

> כללי עבודה שהמשתמש קבע. כל כלל פה חל על **כל פיצ'ר חדש** מאותו סוג, לא רק על המקרה הספציפי שדובר עליו.
>
> כשהמשתמש נותן הערת עיצוב/בנייה — מוסיפים אותה לקובץ הזה כדי שלא תחזור.

---

## 1. רכיבי FAQ / Accordion — תמיד דרך הרכיב הקיים

**הכלל:** כל קטע של שאלות-תשובות, אקורדיון, או תפריט מתקפל באתר חייב להשתמש ב-`@/components/Accordion` הקיים.

**אסור:** להשתמש ב-`<details>` נטיב של HTML או לבנות אקורדיון חדש מאפס.

**למה:** לרכיב הקיים יש אנימציה חלקה (`max-height` עם `transition: 0.35s ease`, סימן + שמסתובב 45°, fade-in לתוכן). פתיחה בקפיצה (jump-cut) נראית רעה ולא אחידה.

**איפה זה נכון:** כל דף שיש בו FAQ — `/`, `/crm`, `/whatsapp-bot`, `/automation`, `/lead-gen`, `/about`, `/faq`, וכל דף עתידי.

**איך מחילים:**
```tsx
import Accordion from '@/components/Accordion'
// ...
<Accordion items={faqs} />
// או עם רקע מותאם:
<Accordion items={faqs} background="#f9f9f7" />
```

---

## 2. עקביות עיצוב חוצת-דפים

**הכלל:** הערת עיצוב שהמשתמש נתן פעם אחת חלה על **כל** רכיב דומה שיבנה אחר כך — לא רק על הרכיב המדובר.

**איך מחילים:**
- לפני בניית רכיב חדש — לבדוק אם כבר קיים רכיב דומה ב-`components/`
- אם כן — להשתמש בו, לא להמציא חדש
- ספציפית: כפתורי CTA, hero sections, stat cards, accordions, תפריטים — לבדוק את הקיים קודם

---

<!-- כללים נוספים יתווספו כאן לפי הצורך -->
